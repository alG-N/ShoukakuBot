export interface PlatformConfig {
    name: string;
    id: string;
    color: string;
    patterns: string[];
    supports: string[];
    maxQuality: string;
}

export interface PlatformInfo {
    name: string;
    id: string;
    color: string;
    supports: string[];
    maxQuality: string;
}
